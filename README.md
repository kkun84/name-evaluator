# Name Evaluator

React製のシングルページアプリケーションで姓名判断を表示します。GitHub Pages向けの静的サイトとして配置でき、同じ名前には常に同じ結果が返るように設計されたアルゴリズムを採用しています。

## 開発

```bash
npm install
npm test
```

`index.html` をブラウザで開くとアプリケーションを確認できます。
